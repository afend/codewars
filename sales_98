-- For this challenge you need to create a SELECT statement, this SELECT statement will use an EXISTS to check whether a department has had a sale with a price over 98.00 dollars.

-- Create your SELECT statement here
SELECT d.id, d.name
FROM departments AS d
WHERE EXISTS
(SELECT s.id, s.name FROM sales AS s WHERE price >= 98.00);


-- For this challenge you need to create a SELECT statement, this SELECT statement will use an IN to check whether a department has had a sale with a price over 98.00 dollars.

-- Create your SELECT statement here
SELECT s.id, s.name
FROM sales AS s
WHERE price >= 98.00 AND s.id IN (SELECT d.id FROM departments AS d) 

SELECT id, name
FROM departments
WHERE id IN (SELECT department_id FROM sales WHERE (price) > 98.00);


-- departments table schema
-- id
-- name
-- sales table schema
-- id
-- department_id (department foreign key)
-- name
-- price
-- card_name
-- card_number
-- transaction_date
-- resultant table schema
-- id
-- name
-- NOTE: Your solution should use pure SQL. Ruby is used within the test cases to do the actual testing.